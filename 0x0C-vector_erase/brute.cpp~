#include <iostream>
#include <vector>

using namespace std;

int main(void)
{
	int lenght = 0, element = 0;
	vector<int> v;

	cin >> lenght;

	for (int i = 0; i < lenght; ++i)
	{
		
		v.push_back(element);
	}



	return 0;
}
